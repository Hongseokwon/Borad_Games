using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bang_Bot2 : Bang_Bot
{
    // Start is called before the first frame update
    void Start()
    {
        Player_Num = 2;
        name = "Bot2";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

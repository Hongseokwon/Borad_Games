using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bang_Bot4 : Bang_Bot
{
    // Start is called before the first frame update
    void Start()
    {
        Player_Num = 4;
        name = "Bot4";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

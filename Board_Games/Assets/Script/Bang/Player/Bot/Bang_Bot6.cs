using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bang_Bot6 : Bang_Bot
{
    // Start is called before the first frame update
    void Start()
    {
        Player_Num = 6;
        name = "Bot6";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
